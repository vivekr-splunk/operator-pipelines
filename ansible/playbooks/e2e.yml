---
- name: Deploy operator-pipeline {{ env }} instance
  hosts: operator-pipeline-{{ env }}
  vars_files:
    - ../vaults/e2e/ocp-token.yml
  roles:
    - e2e
  environment:
    K8S_AUTH_API_KEY: '{{ ocp_token }}'
    K8S_AUTH_HOST: '{{ ocp_host }}'
